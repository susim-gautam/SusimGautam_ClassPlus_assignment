# Optional: override API origin (production). Dev default: same-origin /api via Vite proxy.
VITE_API_URL=
# Google OAuth client ID (Web application) — optional.
# Without this, the app shows "Continue with Google (demo)" instead of the real button.
VITE_GOOGLE_CLIENT_ID=
